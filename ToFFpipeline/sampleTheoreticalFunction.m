function [theoreticalDistGrid] = sampleTheoreticalFunction (fn, N)
    % sample the function over a meshgrid square for each index
    arguments
        fn {isa(surfFn, 'function_handle')}
        N double {isinteger}
    end


end