function [indexSubset] = rejectOcclusionOutliers (distGrid)
    % return a list of indices that are valid for curve fitting using segmentation on the grid data from other views
    arguments
        distGrid (:,:) double
    end

end