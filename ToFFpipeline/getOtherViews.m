function [pixelGrid] = getOtherViews (LF, pixel, K, Pz)
    % shifts through the LF and finds the pixel in other views, returning a matrix of pixel values from other views
    arguments
        LF (:,:,:,:) double
        pixel (2,1) double
        K (3,3) double
        Pz double
    end

end