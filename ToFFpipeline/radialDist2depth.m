function [Pz] = radialDist2depth(radialDist, K, pixel)
    % converts a radial distance to a planar depth from the camera
    arguments
        radialDist double
        K (3,3) double
        pixel (2,1) double
    end
end