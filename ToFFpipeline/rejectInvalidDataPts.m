function [indexSubset] = rejectInvalidDataPts (distGrid)
    % rejects saturated and similar points, returning a logical array of points to sample at
    arguments
        distGrid (:,:) double
    end

    
end