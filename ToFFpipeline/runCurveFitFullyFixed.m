function [fitted_curve, rmse, error, regionCut] = runCurveFitFullyFixed(distSurf, regionCut,d,Pz,K)
    % curve fit using only Pz, since the pixels are already in known
    % locations

    [ii,jj] = meshgrid(1:N);


    
end